{
  "name": "@cloud9-labs/mcp-resend",
  "version": "0.1.0",
  "description": "MCP server for Resend email API - send emails, manage contacts, audiences, and domains",
  "type": "module",
  "bin": {
    "mcp-resend": "./dist/index.js"
  },
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "prepare": "npm run build"
  },
  "keywords": ["mcp", "resend", "email", "ai", "model-context-protocol"],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/cloud9-labs/mcp-resend.git"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.26.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
